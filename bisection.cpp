#include<iostream>
using namespace std;
float f(float x);
float a, b, p, FP, FA, TOL;
int N0, i;
int main(){ 
	
	
	
	i=1;
	
	FA=f(a);
	while i<=N0;
		p=a+(b-a)/2;
		FP=f(p);
		if (FP==0) .or. (b-a)/2<TOL
			std :: cout << p << std :: endl;
		
	
	
}
